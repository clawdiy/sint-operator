# ═══════════════════════════════════════════════════════════
# SINT Marketing Operator — Configuration
# ═══════════════════════════════════════════════════════════

# ─── AI Providers (at least one required) ──────────────────
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...

# ─── Model Overrides (optional) ────────────────────────────
# SINT_MODEL_COMPLEX=o3                    # Complex tasks (strategy, analysis)
# SINT_MODEL_ROUTINE=gpt-4.1-mini         # Routine tasks (formatting, short-form)
# SINT_MODEL_FALLBACK=gpt-4.1-nano        # Fallback on rate limits
# OPENAI_BASE_URL=                         # Custom OpenAI-compatible endpoint

# ─── Server ────────────────────────────────────────────────
PORT=18789
# AUTH_ENABLED=false                       # Enable JWT auth (disabled by default)
# JWT_SECRET=change-me-in-production       # Required if AUTH_ENABLED=true
# PUBLISH_QUEUE_WORKER_ENABLED=true        # Auto-process publish queue in background
# PUBLISH_QUEUE_WORKER_INTERVAL_MS=30000   # Worker interval in ms (5s..5m)

# ─── Social Publishing (optional) ─────────────────────────
# Twitter/X — get from developer.twitter.com
# TWITTER_API_KEY=
# TWITTER_API_SECRET=
# TWITTER_ACCESS_TOKEN=
# TWITTER_ACCESS_SECRET=

# LinkedIn — get from linkedin.com/developers
# LINKEDIN_ACCESS_TOKEN=
# LINKEDIN_PERSON_URN=urn:li:person:XXXX

# ─── Notifications (optional) ─────────────────────────────
# Telegram — get from @BotFather
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_CHAT_ID=

# ─── Webhooks (optional) ──────────────────────────────────
# WEBHOOK_HMAC_SECRET=                    # Preferred webhook signature secret (HMAC SHA-256)
# WEBHOOK_HMAC_TOLERANCE_SEC=300          # Signature timestamp tolerance in seconds
# WEBHOOK_INGEST_SECRET=change-me         # Fallback shared secret if HMAC is not configured

# ─── Dashboard ─────────────────────────────────────────────
# DASHBOARD_URL=http://localhost:18789    # Used in notification links

# ─── Storage ──────────────────────────────────────────────
# SINT_DATA_DIR=./data                    # SQLite database directory
# SINT_CONFIG_DIR=./config                # Pipeline + brand config directory
